# C++ Arc Review

## Learning C++ 17 - 20

### Short answer to C++ 17 - 20

The features made me realize what C++ was designed for. I extremely
underestimated it's use.

### Long answer to C++ 17 - 20

C++ 17 - 20 was a quick reality check of what Rust was competing with.
As I, learning from a Rust beginning, got hit by a wave of a matured,
and coordinated structure. I struggled with understanding the concepts,
but I later got a hold of it.

I could see some use-cases from there, but I'll admit, it tries too much
to contain itself to be close to it's father C.

## Learning RAII

### Short answer to learning RAII

RAII attempts to fix common slips in C++, but not all of them.

### Long answer to learning RAII

RAII is a useful tool for long term use, in a program having very long
lines of code, or even to do something before and after an object's
creation and destruction.

It is very much useful, but not sufficient.

## Learning about STL

### Short answer to reading STL

STL provides generic solutions that are fast for generic objects.

### Long answer to reading STL

I find STL a useful tool to use, for preventing myself from writing
generic structs, algorithms, and iterators for general use.

I connect this with functional methods in Rust, which, on practice, the same.

Though this doesn't solve all problems. As specific structs,
algorithms, and iterators require special solutions.

## Project 1: File Compressor Recreation

### Prelude

In preparation, I planned the structure of the project:

```none
root
|- data.hpp
|  '- OilData Class
|- file.hpp 
|  '- File Manipulation
'- main.cpp
   '- Argument handling
```

I wished to do multiple file compression, but in my scope, that's something
I'd have to reinvent, and comparing to tar's time would've been a good benchmark.
But here, I'm trying to retouch C++, nonetheless, it would have been unfair.
I also wished to challenge it by compressing an executable, or the oil compressor
binary, but for RLE, that is not an efficient way to cut it, and would take more
time to debug, fix, and repeat. As again, I am retouching C++, not showing it off.

Finally benchmark it by timing it with a well known compressor, .zip.

### Coding review

### Remaking the original program

Classes were weird at first, since I am retouching C++.
Splitting them was a good way to let my mind go off things, sometimes
me seeing a big line count makes my brain go; "I think I made it too big".
Though in the end I still think it was big.

My initial mistakes were, primal, to say the least. Misunderstanding
std::optional, std::expected, at first. I got a hold of it after 2 sessions.

After finishing the OilData class with minimal Move semantics. I was good to go
onto file.hpp, there I started with the compile function first, reading C then
translating to Modern C++ tricks was bizarre to me, like I was saying to myself,
"Wow, this really does look like Rust!" and seeing it look like functional code,
from someone who only saw really trash C++ code was, to me, kind of amazing.

Going forward, I was getting tired of my function ordering and misuse of some functions,
and I had to refactor. Starting fresh, I could order it better rather than just pushing
forward, "Doing this" and "Doing that". Kind of like copying from a classroom board;
the teacher just copying from the book, making mistakes and doing nothing about it. Me
on the other hand can fix those mistakes and maybe clear up some things I might not
understand. Or in this case, to all of those who will read it.

Some helpers here and there, just implementing an OilData reader for more readability,
since OilData writers are just fwrite wrappers at that point.

Then constexpr variables were definitely something I'll add to my coding arsenal.
Since they also helped make me point to the correct array element and make it more clearer
to read, I think.

In that stage I implemented some things, OilError for std::expected or std::optional
for better distinction between errors, or successes. Sometimes fixing some OilData bugs.
And using STL objects were definitely a huge help, std::string actually had '+='.
Which I marketed on by using it whenever I had to append. In retrospect, I should have
use '.append()', but it's too late.

Then wrapping this up in main.cpp I found some logic bugs along the way. Reviewing the code
2 times, and researching some C++ shenanigans, the function that handles OilData being
converted to bytes to write had a bug where the returned output was a local reference, and it
was the cause of a segmentation fault. After fixing that, I realized that I passed the wrong
data to distinguish between a Huffman compression, or a Raw compression. And a vector misuse.

Finally it was just smooth sailing from there. Just some C to C++ translations and it was good
to deploy!

### Debugging

It was very hard to debug not going to lie. It was difficult to work with, to be honest. So
from there I messed up:

- Decompilation
- Compilation in general
- Core logic in general

It was a really messy bug hole. But in 2 coding sessions I fixed it! At least...
The core logic in question was how buffers we're both being handled in a huff run. And through
it's rather complex, nested code it was hard to see. It was just previous not being set every
loop, and also how raw was also being intertwined with the huff run so we can get that delicious
1 byte save that was causing the whole newline repeats all over the place.

### Putting in a sensible structure

Looking at CMake, I took the structure from fmtlib's fmt repo and just built it from there.
Though not really a full carbon copy, just took the core structure.

Learning it was somewhat of a breeze? It took just 1 coding session to get it.

So here's the foundation of the project:

```none
root/
|- include/
|   |- oil.hh     [     CORE LIBRARY    ]
|   '- internals/ [  LIBRARY INTERNALS  ]
|       |- files/ [  FILE MANIPULATION  ]
|       '- misc/  [ OTHER TOOLS/HELPERS ]
'-src/
   |- main.cc     [    MAIN/CORE LOGIC   ]
   '- internals/  [ LIBRARY DEFINITIONS  ]
```

And yeah, just some translation mistakes here and there, you get the idea.
And solving UB it was finally good to test it!

### Benchmark results

Quick disclaimer, I'm just doing it as a reference point.
ZIP is much more refined, older, and uses better techniques than my software.
I'm just doing this so I can see what I achieved.

| File used             | Time Compressed ( ZIP - OIL ) | Size ( ZIP - OIL ) |
|-----------------------|-------------------------------|--------------------|
| 100 random characters | 0.025 sec - 0.047 sec         | 268B - 110B        |
| Repeated 1's 0's & \n | 0.002 sec - 0.003 sec         | 190B - 43B         |
| This Review file      | 0.005 sec - 0.005 sec         | 4.2KB - 9.3KB      |

And... Yeah, size is expected. Since ZIP uses a complicated header, which is more
secure and very specific to check for, ensuring a correct file decompressed.

I used /dev/urandom (The linux random text generator) to create the 100 characters.
I suspect the time is because of multiple branching it takes for my algorithm,
so I think that will suffice. The size is still pretty meaningless, no uniform data means
high entropy, and high entropy is a hard surface to compress, so from there it's just
a header comparison at face value.

This review file's compression was almost expected.
Since this review is mostly words, not repeated random text. They timed pretty well,
well accounting for the size, it's almost twice the size. Since this algorithm does not
account for natural text in general, nor was it even designed for it.
So Raw data sections are mostly used, no compression, just translation. ZIP does meaningfully
well here, as it uses other algorithms, it beats my 2 algorithm compressor well.

Time is also really close. Since ZIP is, as said before, a refined, and matured system.
This is a really close race, the difference being C was used for ZIP and mine was C++

### Try OilCompiler yourself

You can download it here and test it yourself!

> GitHub link: [OilCompiler](https://https://github.com/UnfortunateGames/OilCompiler)

### Conclusion

C++ is a really well refined, and matured language environment! I had a good time
reading the documentary, there's literally so much resources on C++ that I can't even
count how many sources covered one feature of C++. Really, I love it's ancient and
popular vibes! Kind of like math, it's books are years/decades old, but they're still
as useful as you can even live for.

## Project 2: My take of libraries (Mock Libraries)

### MockLib Prelude

I will make a small mock libraries.
Those will be SmallVec, Stopwatch, and ScopeGuard

SmallVec will be initialized with a set size.
Then when it overflows that set size, it'll go to a heap pointer.

Stopwatch will be a, of course, timer to see execution time.
It'll only have Start, pause, reset, and stop.

ScopeGuard will only contain 1 function, this'll be called on destruction.
(I can't believe that rhymed.)

This is to make me get used to C++ patterns, not as a useful project in general.
Just something to use as leverage rather than a tool.

### Coding Review

### Making "Stopwatch"

In making stopwatch.
I accidentally made a different kind of timer.
A Kitchen Minder like timer, where you advance it manually

I decided to include it as an accidental tool I could use for later reviewal.

It's a manual timer, as a counter using chrono's measurements.
It has a `seconds` variable, and to make it more special and not boring,
I made it have an alarm system. It's simple, and basic, I'll review this in a later time.

### Making SmallVec

This was quite of a stretch to conceptualize. It started at me thinking of it as a unique,
complicated vector that has a set size, but if it overflows, it flows into a heap pointer.
You can tell I was making this not as simple as it should be. I made it 35% with the idea,
until I just realized this wasn't a really good idea. So I just made into a boring replica.

It's now a simple Vector replica along with the features it would have.
The tests do not test it to it's full extent. Though for a practice/exercise project, I'd opt
not to test it as this is a rather uninteresting copy of what everyone can do.

I would compare this to the tests of Stopwatch, and you'd be right; *"Why test a smaller program*
*that's rather uninteresting, more than to test a replica of a well-known STL?"*. Well, it's just
that it's already boring in the first place. It's already simple enough that the only obstacle are
logic.

### Making ScopeGuard

This is rather boring.

Though I only found just one thing, I will research about this, but to my current knowledge,
I used `using function = void (*)()` as a function representee. I believe this is an explicit
function address that the first parenthesis `(*)` serve as the pointer, and the second `()`
being the function caller.

Then, at it's destruction, we run the function.

Finally, it's just a function handler from there, and nothing else interesting to look for is there.

### Making Stopwatch

Now... Here I made a lot of mistakes.

Well, looking at it now, it seems to just be because of inexperience with chrono.

So the first mistakes we're just confusion on what to even do.

What do I even get? Should I use the measurements from before, or what? Though looking at it now,
I should have really taken the cppreference in mind. I really thought chrono gave the functionality,
not the measurements on how to create it.

So, right after I figured it out via cppreference, it being a clock isn't really it's job.
It only gives us the measurements, and the time now. Still, the ride was bumpy.

My logic was wobbly. I tried formatter first then manipulation, it was wrong because, a clock can't
exist first without it's gears controlling it's time.

Then when I figured it out, I still made the logic bad. I subtracted the stopwatch's birth to the time
it was being checked, it was the other way around, the time checked then, subtracted by it's birth giving
us the duration. Everything else was rather fine.

Then came to the formatter. This felt like learning Rust again, since, I reverted back to C problem solving:
*"If it compiles and the logic works in your head, it gets the idea around"* I used `snprintf` and a `char*`.
Of course, strings were there for a reason, they couldn't have been there for show, right? So when I finally
found out about the `std::to_string` function it was pretty straight forward from there.

The format to a `std::string` was made out from the start. To differentiate the two twin clocks. I made a
different format for them, the minder having; *"HH:MM:SS"*, and the stopwatch having: *"?h ?m ?s"*

### Try MockLib yourself

You can test them here, and try to break it!

I made some tests myself for them myself, you can try them out yourself!

> GitHub link: [MockLib](https://github.com/UnfortunateGames/MockLib)

### MockLib Conclusion

It was really fun making the tests for these.

Really a good exercise I must say. I should really do this more often for learning libraries. It's much more
informative, and just aligns with my *"Doing while learning"* learning strategy, and it works best! How
do you think I learned these programming languages in the first place?

No matter how many times you ask me, it was fun.

## Project 3: ServRViewR (Rust and C++ observations)

### ServRViewR Prelude

I will make a C++ replica of my Rust ServRLookR

> Original [ServRLookR](https://github.com/UnfortunateGames/ServRLookR)

Then observe the differences of both Rust and C++. Note that I'm not making the project itself better, I'm just
going to observe it's overall structure and how it will be made. Alongside with learning STL, I haven't fully
touched on STL, so this'll be an STL-heavy project, as similar to the original one.

### Making the libraries

The libraries were rather simple. A bit of reconstruction from the original was really easy.

I had to sort them a bit, as the original had no separate dependency folder. I didn't mind
much from the structure, as they were mostly public, and to be used by the main logic. The
only potential private was commands, I don't think it affects anything that much, so I
decided to just make it private because I can.

Here's the structure if you're keeping track:

```none
root/
|- include/
|   | [ Declarations ]
|   |- cmd/
|   |   '-commands.hh      [ Command Struct ]
|   |- data.hh             [  Every Struct  ]
|   |- inpututil.hh        [ In ServRLookR  ]
|   '- parsers.hh          [  Input Parser  ]
'- src/
    |  [ Definitions ]
    |- cmd/
    |   '- commands.cc
    |- input/
    |   '- inpututil.cc
    |- parsers/
    |   '- parsers.cc
    '- main.cc             [   Main Logic   ]
```

I questioned beforehand of there being a utility library, if it was even accepted or allowed.
Surprisingly, it only applies to classes, shocking to me I guess. Moving on, originally I was
going to make all of them public because, why? Though later on, I decided to make it apply to
my standard of, "Why not?" The code behavior is excluded from this of course.

Nothing special happened next, that's all that is, in the making of the libraries, structure,
etc. Same goes with the main logic, language reconstruction is easy.

### My thoughts on debugging

Now unto debugging, I have to be serious with you, how easy do you think reading code is? Easy?
Maybe, but for me that was a headache to even read. Maybe it was because I was using a 1.15
line height in my code editor, but the pure density in the C++ code made me give up from time
to time whenever I decided to try coding.

I would guess that it is because the language was designed for control, to trade from
convenience. In contrast to this, take Rust as an example, they made Rust for better functional
control, and look where the code structure landed. I would guess again that maybe this is more
complicated than I made it to be, and there's a function out there that solves the manual
splitting I've made. I doubt it, really.

I made a lot of code adjustments, and a lot of them were in a dense cloud of code. I do hope
that I could improve myself on this, whether I tolerate it, or better myself from writing bad
code, and I wish to avoid getting demoralised by this.

### Unprecendented Dissapointment

After the rather small debugging of the libraries, and the fixing of me changing the parameters
of the definition, and not updating the declariation was a bit of a mind teaser, due to how
cryptic `ld` is at error verbosity. I simply had to read the very beginning of the error message
to know what was wrong.

To put it simply, I haven't had a single experience of `ld` failing on me, and in result of that
inepxerience, I had no idea what to look at to see where I went wrong. Learning from that, I
should definetely adjust my workflow to this, it's a bit of a rookie mistake to do this.

Now there is one thing I haven't mentioned, and if you read all the sub-chapters before-hand, I
think I don't have to say anything, other than I really have to get some core components through
and not treating them as translation.

### Implementing Splitting

There is something to say about this, but either way, it's basically the same as the
*My thoughts on debugging* chapter. Why isn't there a library solution to this? Why can't it be
convenient as the other STL's?

As much as I want to vent out my frustration on splitting. We kind of have to move on from this,
there's a whole sub-chapter where I complain about it, and you can see how impractical it is to
also dedicate another because "I feel like it."

Either way, splitting was a pretty rudimentary process. A vector of indexes (`size_t`), that
acts a sort of coordinates to the command. To be more specific, an array of size 4, is in the
vector, that contains the indexes, 2 indexes for each command part; the main command and the
parameter. So there I used `string_view`, a sort of scope that doesn't copy the string, like
pointers for example. I made some cases immediently return, like if the command is completely
empty, and some cases to errors, like obviously invalid commands, parameters, and no parameters.

I went on a long and stupid journey to actually figure out I need 4 instead of 2-3 indexes.
Continuing on the debugging, I took really long to really do it properly, as I said, the code
became dense, same as what I've said in *My thoughts in Debugging*. That was until I used something
I felt avoiding which was writing on a separate .txt and checking the logic from there, then it
immediently became obvious to me that I was missing something, I was depending on a uninitialized
command begin index. So, until then, I was dissapointed in myself for letting myself keep that happening.

### ServeRViewR Conclusion

I see that C++ isn't an exception in bad code. I do think there are code-bases out there that
might be more dense than my project, even if it's a stretch since mine is smaller, and teams
are more usually competent at clean code structure, and knowledge better than me.

I have seen my mistakes, habits, and strategies to overcome my bad habits to better myself, and
hoping I do become better, I don't see myself repeating the same mistakes such as what I did here.
It's more of an "*Oopsie!*", rather than "*I messed up...*". I'm going to treat it like Bob Ross
did to his paintings, it's a happy little accident.

## Reflection

Alright, we can finally conclude this quarter, I'll provide a QNA to bring some things I skipped in the
past sections to paint a clearer picture of what I've experienced.

[ Questions & Answers ]

1. Why **C++** as your *first journey*?

   > Because I think it's hard, I mean many people think it is, so I had to face it first to make
   > myself more causious, and more knowledgable of what I'm doing.

2. According to your goal, what do you think is the **main difference** between *Rust* and *C++*?

   > C++ prioritizes control, similarly to my splitting implementation in ServRViewR, I had to
   > implement a command splitter, it provides more room to be different, and more room for
   > error, like my mistake in splitting logic. Rust on the other hand, provides a larger
   > department of abstractions for common uses, like a more versatile STL. More convenience,
   > allowed me to do splitting without much thinking in the original ServRLookR.

3. What **habits** or **mistakes** do you *knowingly* want to avoid?

   > The main thing I do want to avoid is trying to solve a problem that is complicated, and me
   > refusing to simplify it. A prime example of this is the way I handled the ServRLookR splitter
   > problem, I tried to find the problem in the raw code, the syntax was distracting me, this made
   > me waste time to actually see the simple part that could've fixed everything.

4. What *solutions* have you found **useful**?

   > Reconstruction, either in pseudocode first, or a different simpler language. I find it
   > more easy to apply structure in a viewable logic, since I can't see logical issues in the code
   > immediently, and check for bugs. This directly closese off the last issue, where I couldn't
   > find the issue clearly due to a large codebase already made.

5. After this, how would you *strategize* your **code building**?

   > I'll apply what I've learned; plan it's logic in a clear concise text, then isolate core
   > components to test them separately to check for bugs, and flaws, once I'm sure it works fine,
   > I integrate it into the codebase.
